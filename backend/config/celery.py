# This file has been removed as part of the Celery → Django-Q migration.
# The weekly progress summary schedule is now configured via Django-Q's
# Schedule model in apps/progress/apps.py (AppConfig.ready).
# Run the worker with: python manage.py qcluster
