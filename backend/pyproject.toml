[tool.isort]
profile = "black"

[tool.black]
line-length = 88
target-version = ["py39"]

[tool.coverage.run]
source = ["backend/apps"]
omit = ["*/migrations/*", "*/tests/*", "*/admin.py"]

[tool.coverage.report]
fail_under = 70
show_missing = true
